CFLAGS += -std=gnu99
LDFLAGS += Std.rlib Riva.rlib $(file Tre.rlib) Gmp.rlib

if $(equal $(OSTYPE), Cygwin)
	#LDFLAGS += advapi32.rlib
	export

RivaModule(Util/TypedFunction, TypedFunction)
#RivaModule(Util/Regexp, Regexp)
RivaModule(Util/Base64, Base64)

if $(equal $(SYSNAME), Linux)
	mkdir(-p Tecla Editline)
	.SUBDIRS: Tecla Editline

WraplModule(Util/Archive, Archive)
WraplModule(Util/Persist, Persist)
WraplModule(Util/Pattern, Pattern)
